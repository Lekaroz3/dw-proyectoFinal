{{ form_start(form) }}

    
        <div class="container">
        <div class="row">

            <div class="col-8 mx-auto">

                <div class="card">
                    <div class="card-body">
                        <h3 class="card-title">Categoria</h3>
                        <div class="card-text">
                            <div class="border border-white row">
                                <label for="vName" class="col-sm-2 col-form-label"></label>
                                <div class="col-sm-10">
                                    {{ form_widget(form) }}
                                </div>
                            </div>
                            <button class="btn btn-primary">{{ button_label|default('Save') }}</button>

                            
                        </div>

                        </div>
                        {{ form_end(form) }}
                    </div>

                </div>

            </div>
        </div>
    </div>
